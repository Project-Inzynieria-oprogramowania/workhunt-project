<!DOCTYPE html>
<html>
  <head>
    <title>WorkhuntProject</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'logo.png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "common", "header", "form", "signup", "startpage", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "flash" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'home/flash' %>
          <% if controller_name == 'sessions' && action_name == 'new' || controller_name == 'users' && action_name == 'new' %>
              <%= render 'home/header' %>
          <% elsif controller_name != 'home' %>
              <%= render 'home/headerAlt' %>
          <% else %>
            <% if user_signed_in? %>
              <%= render 'home/headerAlt' %>
            <% end %>
          <% end %>
    <%= yield %>
  </body>
</html>
